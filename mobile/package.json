{
  "name": "opsbrief-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build-www": "node -e \"const fs=require('fs'); fs.existsSync('www') && fs.rmSync('www',{recursive:true}); fs.cpSync('../frontend','www',{recursive:true,force:true})\"",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/browser": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
