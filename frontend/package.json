{
  "name": "opsbrief",
  "version": "1.0.0",
  "description": "OpsBrief - Daily Intel for IT Professionals",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed for static PWA'",
    "sync": "npx cap sync",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
